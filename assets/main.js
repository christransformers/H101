var heroIndex=0;const slides=document.querySelectorAll('.hero-slide');function go(n){const len=slides.length;slides[heroIndex].classList.remove('active');heroIndex=(n+len)%len;slides[heroIndex].classList.add('active');document.querySelectorAll('.hero-dot').forEach((d,i)=>d.classList.toggle('active',i===heroIndex))}setInterval(()=>go(heroIndex+1),6000);